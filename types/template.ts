export interface TemplateDto {
  id: string;
  name: string;
  body: string;
  language: string;
  description?: string;
}

export interface Template {
  id: string;
  name: string;
  body: string;
  language: string;
  description?: string;
}
